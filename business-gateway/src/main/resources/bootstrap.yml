spring:
  application:
    name: business-gateway
  profiles:
    active: test
  cloud:
    nacos:
      config:
        server-addr: http://192.168.1.8:8748
        enabled: true
        file-extension: yml
        group: cloud

